{
  "name": "multi-ai-at-once",
  "version": "1.0.0",
  "description": "A platform that sends prompts to multiple AI services simultaneously",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "clean": "turbo run clean",
    "cli": "pnpm --filter @multi-ai/cli cli",
    "web": "pnpm --filter @multi-ai/web dev"
  },
  "keywords": ["ai", "automation", "playwright", "cli"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "turbo": "^2.3.3"
  },
  "dependencies": {
    "playwright": "^1.49.1"
  },
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
